
services:
  # guacd
  guacd:
    container_name: guacd
    image: guacamole/guacd
    networks:
      - npm
    restart: always
    volumes:
    - drive:/drive:rw
    - record:/record:rw

  # postgres
  postgres:
    container_name: guacamole_database
    environment:
      PGDATA: /var/lib/postgresql/data/guacamole
      POSTGRE_DB: guacamole_db
      POSTGRE_PASSWORD: '${POSTGRES_PASSWORD}'
      POSTGRE_USER: '${POSTGRES_USER}'
    image: postgres:15.0
    networks:
      - npm
    restart: always
    volumes:
    - ./init:/docker-entrypoint-initdb.d:ro
    - db:/var/lib/postgresql/data:rw

  # guacamole
  guacamole:
    container_name: guacamole
    depends_on:
    - guacd
    environment:
      GUACD_HOSTNAME: guacd
      POSTGRE_DATABASE: guacamole_db
      POSTGRE_HOSTNAME: postgres
      POSTGRE_PASSWORD: '${POSTGRES_PASSWORD}'
      POSTGRE_USER: '${POSTGRES_USER}'
      POSTGRE_AUTO_CREATE_ACCOUNTS: 'true'
    image: guacamole/guacamole
    networks:
      - npm
    volumes:
      - record:/record:rw
    ports:
      - 8089:8080
    restart: always
  
  

   
volumes:
  drive:
  record:
  db:
  
networks:
  npm:
    external: true
